[project]
name = "livros-app"
version = "1.0.0"
description = "Aplicação para registro de livros lidos."
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"